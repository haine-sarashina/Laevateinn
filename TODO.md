## ✅ 完了済み
### 🔐 認証とセキュリティ (バックエンド)
- [x] OAuth2 フローの実装 (フロー開始 $\rightarrow$ コード交換 $\to$ トークン保存)。
- [x] セキュアなトークン保存 (`keyring` クレートを使用した OS ネイティブな保存)。
- [x] トークン更新ロジック (`refresh_token` を使用した自動更新)。
- [x] 複数アカウント管理の基盤 (認証情報の管理構造)。

### 📧 Gmail API 連携 (バックエンド)
- [x] メッセージ一覧取得 (Gmail API からのメッセージサマリー取得)。
- [x] メッセージ詳細取得 (メッセージの件名やスニペットの取得)。
    - *注: 複雑なヘッダーのパース実装は進行中。*

### 🏗️ インフラストラクチャ
- [x] Tauri プロジェクトのセットアップ (v2, Rust バックエンド, Svelte フロントエンド)。
- [x] プロジェクト構造の構築 (コマンド/モジュールの分離)。
- [x] ビルド/開発パイプライン (pnpm, tauri dev, tauri build)。

---

## 🚀 進行中
### 🛠️ バックエンドの洗練
- [x] **Gmail ペイロードの完全なパース**: 複雑な Gmail API の JSON 構造から、より詳細なメタデータ (From, Date, Body snippets) を抽出。
- [x] **Rust 型定義・コマンドの修正**: `gmail.rs` の型不rg不整合および `auth.rs` の構文エラーの解消。
- [x] **エラーハンドリングとロギング**: Rust と Svelte 間の統一されたエラー報告システムの構築。


### 🎨 フロントエンド開発 (Svelte)
- [x] **アカウント管理 UI (サイドバー)**:
    - [x] アカウント一覧の表示。
    - [x] アカウント切り替え (Rust コマプリを呼び出してアクティブなトークンを切り替え)。
    - [x] 「アカウント追加」ボタン (OAuth2 フローの開始)。
        - [x] 認証状態（ログイン済み/未ログイン）に応じた表示の切り替え。
        - [x] 認証完了後の自動アカウント追加。
- [x] **メール一覧ビュー (メインコンテンツ)**:
    - [x] メッセージ一覧の表示 (ページネーション)。
    - [x] ローディング状態とエラー表示 UI。
- [x] **メール詳細ビュー**:
    - [x] 詳細なメール表示 (件名, 本文, スニペット)。
    - [ ] スレッド表示 (必要に応じて)。
- [ ] **メール作成 UI**:
    - [ ] シンプルなメール作成インターフェース。

### 🔄 統合と状態管理
- [ ] **Svelte Store の実装**: 「アクティブなアカウント」の状態をグローバルに管理。
- [ ] **状態の同期**: バックエンドでのトークン更新やアカウント切り替え時に、UI を自動的にリフレッシュする仕組み。

---

## 📅 今後のロードマップ
- [ ] **IMAP/SMTP サポート**: Gmail API を超えて、他のプロバイダへの対応。
- [ ] **高度な検索機能**: Gmail 特有の検索クエリを Rust バックエンド経由で実行。
- [ ] **添付ファイル対応**: 添付ファイルのテンポラリなダウンロードと表示。
- [ ] **パフォーマンス最適化**: 大規模なメールボックスに対する高度なキャッシュ/破棄戦略の導入。

---

## 🐛 既知の問題点 (2026-04-18 現在)

### 🔴 ブロッティング問題

#### 1. Tauri v2 の popup ウィンドウバグ (未解決)
- **現象**: `WebviewWindowBuilder` で作成した OAuth popup ウィンドウ ("Google Sign-In") が白 screen に成る
- **原因**: Tauri v2 (React Native WebView2 backend) の既知のバグ
- **回避策**: popup ウィンドウを削除し、メインブラウザでOAuthフローを実装済み
- **対応状況**: **回避策は実装済み、但尚コードクリーンアップが必要**

#### 2. Tauri.exe ファイルロック (未解決)
- **現象**: ビルド時に `failed to remove file Laevateinn.exe` エラー (os error 5)
- **原因**: 前のTauriプロセスがExeファイルをロックしている
- **回避策**: タスクマネージャーまたはProcess Explorerで古い`Laevateinn.exe`プロセスを終了
- **対応状況**: **手動対応が必要**

#### 3. callback_server ポート62000の競合 (未解決)
- **現象**: ポート62000が既に使用されている
- **原因**: 前のcallback_serverプロセスがポートを解放していない
- **回避策**: `netstat -ano | findstr 62000`でPIDを特定後、タスクマネージャーで終了
- **対応状況**: **手動対応が必要**

### 🟡 今後の修正が必要

#### 4. OAuth popup 関連コードのクリーンアップ
- **対象ファイル**: `src-tauri/src/commands/auth.rs` の `open_oauth_window` 関数
- **内容**: 使用されていない関数の削除、コメントの更新
- **優先度**: 中
- **対応状況**: **完了済み** - `open_oauth_window` 関数、`lib.rs` でのインポート・コマンド登録を削除

#### 5. OAuth フローの最終テスト
- **内容**: メインブラウザ経由のOAuthフローが完全に動作するか確認
- **タイミング**: PC再起動後、プロセス競合を解消後
- **優先度**: 高

#### 6. フロントエンドの初期表示問題
- **現象**: 初回起動時に白 screen になる場合がある
- **原因**: ビルドアーティファクトが生成されていない
- **回避策**: `pnpm run build` でビルド artefact を生成
- **対応状況**: **既存のワークアラウンド**

---

*最終更新: 2026-04-18*
