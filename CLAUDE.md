# CLAUDE.md

このファイルは、このリポジトリのコードを扱う際の Claude Code (claude.ai/code) へのガイドラインを提供します。

## プロジェクトのビジョン
Tauri と Svelte で構築された、高性能でメモリ効率の良い、カスタムデスクトップメールクライアント。
- **コア原則**: ローカルのフットプリントを最小限に抑える。メールデータはサーバーから取得し、永続的に保存しない（ステートレス/キャッシュのみのアプローチ）。
- **アーキックテクチャ**: カスタム UI (Svelte) と、重要なロジックを処理する強力なバックエンド (Rust)。
- **主な機能**: OAuth2 と OS レベルの安全なストレージを使用した、複数のメールアカウントの同時管理サポート。

## 開発コマンド
- **フロントエンド開発**: `pnpm run dev`
- **Tauri 開発**: `pnpm run tauri dev`
- **Tauri ビルド**: `pnpm run tauri build`
- **リンティング**: `pnpm run lint`

## アーキテクチャの概要 (カスタム UI アプローチ)
- **フロントエンド (Svelte)**:
  - **サイドバー**: アカウントリストとアカウントスイッチャー。
  - **メインコンテンツ**: メールリスト、メール詳細ビュー、および作成ビュー。
  - **状態管理**: 現在選択されているアカウントとメールデータを管理するための Svelte Stores。
- **バックエンド (Rust/Tauri)**:
  - **アイデンティティ & 認証**: OAuth2 フローを処理し、トークンのために OS ネイティブの安全なストレージ (Keychain/Credential Manager) と連携する。
  - **通信**: Gmail API (または IMAP/SMTP) のプロキシとして機能し、メールデータの取得と処理を行う。
  - **セキュリティ**: 安全な認証情報の取り扱いを実装し、平文での保存を防止する。
- **データフロー**:
  - ユーザーが Svelte でアカウントを選択 $\rightarrow$ Svelte が Rust コマプリを呼び出す $\rightarrow$ Rust が安全なストレージからトークンを取得 $\rightarrow$ Rust が Gmail API からデータを取得 $\rightarrow$ Rust が解析された JSON を Svelte に返す。

## 主要な制約とルール
- **応答言語**: 常に日本語を使用すること。
- **ファイル編集**: 編集前にtypoを確認する。ファイルの `Update` が失敗する場合は、`Read` で内容を確認してから `Write` で書き直すこと。
- **問題解決の粘り強さ**: 3回繰り返しても問題が解決しない場合は、アプローチや方法を変更することを検討すること。
- **メモリ効率**: 大規模なメモリ内オブジェクトを避ける。ページネーションと効率的なデータキャッシュ/エビクション（破棄）を実装すること。
- **セキュリティ**: 認証情報を平文で保存しないこと。トークンには常にシステムネイティブの安全なストレージを使用すること。
- **パフォーマンス**: UI スレッドの応答性を維持するため、重いネットワーク処理やデータ解析は Rust で行うこと。
